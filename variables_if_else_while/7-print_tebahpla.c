#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: assign a random number to the variable
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ascii;

	ascii = 123;

	while (ascii != 97)
{
		ascii--;
		putchar(ascii);
}
		putchar('\n');
		return (0);
	}
