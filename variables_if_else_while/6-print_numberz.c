#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: assign a random number to the variable
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ascii;

	ascii = 48;

	while (ascii != 57)
{
		ascii++;
		continue;
		putchar(ascii);
}
		putchar('\n');
		return (0);
	}
